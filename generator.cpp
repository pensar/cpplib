#include "generator.hpp"

namespace pensar_digital
{
    namespace cpplib
    {
        //std::ostream& operator << (std::ostream& os, const IGenerator& g) { return g.write(os); }
        //std::istream& operator >> (std::istream& is,       IGenerator& g) { return g.read (is); }
    }
}